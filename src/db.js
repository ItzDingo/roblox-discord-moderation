const { Pool } = require('pg');
const pool = new Pool({ connectionString: process.env.DATABASE_URL, ssl: process.env.NODE_ENV === 'production' ? { rejectUnauthorized: false } : false });
async function initDb(){await pool.query(`CREATE TABLE IF NOT EXISTS bans (id BIGSERIAL PRIMARY KEY,user_id BIGINT NOT NULL,username TEXT NOT NULL,display_name TEXT NOT NULL,reason TEXT NOT NULL,moderator_id TEXT NOT NULL,moderator_name TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'pending',created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),executed_at TIMESTAMPTZ,discord_channel_id TEXT,discord_message_id TEXT,error_message TEXT,updated_notified BOOLEAN NOT NULL DEFAULT FALSE); CREATE INDEX IF NOT EXISTS bans_user_status_idx ON bans(user_id,status); ALTER TABLE bans ADD COLUMN IF NOT EXISTS error_message TEXT; ALTER TABLE bans ADD COLUMN IF NOT EXISTS updated_notified BOOLEAN NOT NULL DEFAULT FALSE;`)}
async function active(userId){const r=await pool.query("SELECT * FROM bans WHERE user_id=$1 AND status IN ('pending','active','unban_pending') ORDER BY id DESC LIMIT 1",[userId]);return r.rows[0]||null}
async function createBan(x){const r=await pool.query(`INSERT INTO bans(user_id,username,display_name,reason,moderator_id,moderator_name,status) VALUES($1,$2,$3,$4,$5,$6,$7) RETURNING *`,[x.userId,x.username,x.displayName,x.reason,x.moderatorId,x.moderatorName,x.status||'pending']);return r.rows[0]}
async function setDiscordMessage(id,channel,message){await pool.query('UPDATE bans SET discord_channel_id=$2,discord_message_id=$3 WHERE id=$1',[id,channel,message])}
async function setStatus(id,status,errorMessage){await pool.query('UPDATE bans SET status=$2,error_message=$3,updated_notified=FALSE,executed_at=CASE WHEN $2 IN (\'active\',\'unbanned\') THEN NOW() ELSE executed_at END WHERE id=$1',[id,status,errorMessage||null])}
async function getPending(){const r=await pool.query("SELECT * FROM bans WHERE status IN ('pending','unban_pending') ORDER BY id ASC");return r.rows}
async function getById(id){const r=await pool.query('SELECT * FROM bans WHERE id=$1',[id]);return r.rows[0]||null}
async function getRecentCompleted(){const r=await pool.query("SELECT * FROM bans WHERE status='unbanned' AND executed_at > NOW() - INTERVAL '2 minutes' ORDER BY executed_at DESC");return r.rows}
async function getRecentResolved(){const r=await pool.query("SELECT * FROM bans WHERE status IN ('active','unbanned','failed') AND discord_message_id IS NOT NULL AND updated_notified IS NOT TRUE ORDER BY id ASC");return r.rows}
async function markNotified(id){await pool.query('UPDATE bans SET updated_notified=TRUE WHERE id=$1',[id])}
module.exports={pool,initDb,active,createBan,setDiscordMessage,setStatus,getPending,getById,getRecentCompleted,getRecentResolved,markNotified};
